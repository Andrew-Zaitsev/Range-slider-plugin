import Presenter from '../../../../src/facade/presenter/presenter';

describe('test class Presenter', () => {
  // const parentElem: HTMLElement = document.createElement('div');
  // const facade = new Facade(parentElem, {});

  it('Presenter should be a function', () => {
    expect(Presenter instanceof Function).toBe(true);
  });

  xdescribe('test public methods', () => {
    it('test subscribeToModel method', () => { expect(false).toBe(true); });
    it('test disableSlider', () => { expect(false).toBe(true); });
    it('test enableSlider method', () => { expect(false).toBe(true); });
    it('test deleteSlider method', () => { expect(false).toBe(true); });
  });
});
